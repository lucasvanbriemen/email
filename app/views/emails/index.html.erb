<% content_for :title, "Email" %>

<div class="container">
  <%= @emails.each do |email| %>
    <div class="box">
      <p><strong>To:</strong> <%= email.to %></p>
      <p><strong>Subject:</strong> <%= email.subject %></p>
      <p><strong>Body:</strong> <%= email.html_body %></p>
    </div>
  <% end %>
</div>